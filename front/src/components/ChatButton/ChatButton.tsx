const ChatButton = () => {
   return (
      <>
         <button className="fixed rounded-full">
            <img src="/chatbot.svg" alt="ChatBot" />
         </button>
      </>
   );
};

export default ChatButton;
